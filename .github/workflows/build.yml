name: Build apparel-preview

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022

    env:
      VCPKG_ROOT: ${{ github.workspace }}\extern\src\vcpkg
      VCPKG_TARGET_TRIPLET: x64-windows-static-md
      VCPKG_BUILTIN_BASELINE: fab1c6dc7a944372a90a1e19ab5e8e32cd658fc2

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Fetch vcpkg baseline
        working-directory: ${{ env.VCPKG_ROOT }}
        shell: pwsh
        run: |
          git fetch origin ${{ env.VCPKG_BUILTIN_BASELINE }}
          git rev-parse --verify "$env:VCPKG_BUILTIN_BASELINE^{commit}"

      - name: Bootstrap vcpkg
        shell: cmd
        run: ${{ env.VCPKG_ROOT }}\bootstrap-vcpkg.bat

      - name: Install dependencies
        run: ${{ env.VCPKG_ROOT }}\vcpkg install
          --triplet ${{ env.VCPKG_TARGET_TRIPLET }}
          --overlay-triplets=.\cmake\

      - name: Verify CommonLibSSE-NG version
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $expected = '6.8.0'
          $installed = & "$env:VCPKG_ROOT\vcpkg.exe" list commonlibsse-ng `
            --triplet "$env:VCPKG_TARGET_TRIPLET"

          if ($LASTEXITCODE -ne 0) {
            throw "vcpkg list failed for commonlibsse-ng"
          }

          $installed | ForEach-Object { Write-Host $_ }

          if ($installed -notmatch `
              '^commonlibsse-ng:x64-windows-static-md\s+6\.8\.0(?:#\d+)?\b') {
            throw "Expected commonlibsse-ng $expected for $env:VCPKG_TARGET_TRIPLET; actual result shown above."
          }

      - name: Configure
        run: >
          cmake
          -S .
          -B build
          -G "Visual Studio 17 2022"
          -A x64
          -DCMAKE_TOOLCHAIN_FILE=${{ env.VCPKG_ROOT }}\scripts\buildsystems\vcpkg.cmake
          -DVCPKG_TARGET_TRIPLET=${{ env.VCPKG_TARGET_TRIPLET }}
          -DVCPKG_MANIFEST_INSTALL=OFF

      - name: Build
        run: >
          cmake
          --build build
          --config Release
          --parallel
          --verbose

      - name: Assemble install directory
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $dllName = 'ApparelPreview.dll'

          $dlls = @(
            Get-ChildItem `
              -Path build `
              -Recurse `
              -File `
              -Filter $dllName
          )

          if ($dlls.Count -ne 1) {
            $dlls | ForEach-Object { Write-Host $_.FullName }
            throw "Expected exactly one $dllName under build/"
          }

          $dataDir = Join-Path $PWD 'package\data'

          New-Item -ItemType Directory -Force -Path package | Out-Null

          Copy-Item `
            -LiteralPath dist `
            -Destination $dataDir `
            -Recurse `
            -Force

          $pluginDir = Join-Path $PWD "package\data\SKSE\Plugins"

          New-Item -ItemType Directory -Force -Path $pluginDir | Out-Null

          Copy-Item `
            -LiteralPath $dlls[0].FullName `
            -Destination $pluginDir `
            -Force

      - name: Upload Apparel Preview install
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: "Apparel Preview"
          path: package/
          if-no-files-found: error
